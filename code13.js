function squareArea(A){
   const s = A * 4 / (Math.PI * 1);
   return +(s * s).toFixed(1);
 }

//  // function squareArea(A){
//    const s = A * 4 / (Math.PI * 1);
//    return +(s * s).toFixed(1);
//  }
