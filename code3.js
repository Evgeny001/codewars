function find_average(array) {
   // your code here
   return array.length ? array.reduce((sum, value) => sum + value, 0) / array.length : 0;
}
function find_average(array) {
   // your code here
   return array.length ? array.reduce((sum, value) => sum + value, 0) / array.length : 0;
}
